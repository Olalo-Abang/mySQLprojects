select * from customer
where customer_name~* '[a-z]{5}\s(a|b|c|d)[a-z]{4}$';

create table zipcode_table(
pin_codes int);
insert into zipcode_table(pin_codes) values
     (234432), 
     (23345),
	 (123&3),
	 (67424),
	 (7895432),
	 (12312);
select pin_codes from zipcode_table;





























































































































































































































































































































































































































